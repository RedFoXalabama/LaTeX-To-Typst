\documentclass{article}
\usepackage{graphicx} % Required for inserting images
\usepackage{hyperref}
\usepackage{listings}
\usepackage{comment}

\title{LaTeX2Typst Documentation}
\author{Gianfranco Baccarella e Vito Di Bari}
\date{13 April 2026}

\begin{document}

\maketitle
\tableofcontents

\section{Introduzione}

LaTeX rappresenta un potente linguaggio di markup attualmente utilizzato in numerosi contesti, soprattutto accademici. 
La sua potenza rappresentativa permette di realizzare di documenti di testo secondo regole di formattazione e sintassi ben definite. 

LaTeX è definito come un linguaggio di markup di tipo WYSIWYM (What You See Is What You Mean), ossia all'utente è richiesto di redigere un file sorgente che sarà successivamente interpretato da un compilatore dedicato per produrre il documento finale.
Questo approccio risultare può risultare troppo complesso per chi si approccia per la prima volta ad un nuovo strumento per la formattazione di testi differente dai classici programmi come Word di Microsoft. 

Typst è un nuovo linguaggio di mark-up che presenta una sintassi in generale più semplice rispetto a quella di LaTeX e che vanta di una serie di funzionalità built-in, ossia disponibili senza la necessità di importare package specifici, come le immagini o i blocchi di codice.

Da questo nasce l'idea di creare un compilatore che sia in grado di tradurre un file sorgente scritto in linguaggio LaTeX nel corrispettivo scritto in linguaggio Typst. Il compilatore è stato modellato secondo un'architettura a due fasi:
\begin{itemize}
    \item back-end: trasforma il file sorgente scritto in LaTeX in una \textit{rappresentazione intermedia} (IR), basandosi sulle regole di sintassi proprie di LaTeX;
    \item front-end: trasforma la IR nel file Typst.
\end{itemize}

In questo caso specifico, la IR è stata implementata attraverso un Abstract Syntax Tree.

\subsection{Lavori correlati}
\paragraph{\href{https://github.com/scipenai/tylax}{Tylax}}

Realizzato dall'utente \href{https://github.com/scipenai}{scipenai}. Si tratta di un progetto analogo, che permette di tradurre file LaTex in Typst e viceversa. Si tratta di progetto ancora in fase sviluppo, ma che si trova in una fase più avanzata rispetto a quello proposto da questo lavoro e che permette già di tradurre molte strutture sintattiche, compresa una sezione separata esclusiva per la matematica.

Tuttavia, al momento, non supporta la traduzione di alcuni comandi che sono stati gestiti dal presente lavoro, come: \texttt{maketitle} per la realizzazione del titolo e alcuni blocchi, l'environment \texttt{comment} per creare blocchi di commenti e \texttt{flush} per l'allineamento del testo.

È possibile provare una demo del software navigando verso la \href{https://convert.silkyai.cn}{pagina dedicata}. 

\section{Scope del traduttore}
Prima di passare allo sviluppo vero è proprio, è stato definito un documento che delinea lo lo scope del traduttore, ossia tutti i costrutti/comandi/environent che saranno supportati in questa prima versione dello strumento LaTeX2Typst.

La lista delle feature supportate è contenuta in Tabella 1 ed è basata sull'elenco \textit{LaTeX essentials} riportato nella \href{https://www.overleaf.com/learn}{documentazione ufficiale di Overleaf}.

\begin{table}[ht]

\caption{Tabella di compatibilità delle feature (LaTeX \textrightarrow Typst)}

\begin{tabular}{|l|c|}
\hline
\textbf{Feature} & \textbf{Status} \\ \hline
Struttura Documento & Supportato \\ \hline
Metadati e Titoli & Supportato \\ \hline
Sezionamento & Supportato \\ \hline
Formattazione Testo & Supportato \\ \hline
Simboli Speciali & Supportato \\ \hline
Allineamento & Supportato \\ \hline
Liste ed Elenchi & Supportato \\ \hline
Matematica & Supportato \\ \hline
Codice & Supportato \\ \hline
Tabelle & Supportato \\ \hline
Figure & Supportato \\ \hline
Link & Supportato \\ \hline
Spazi e Interruzioni & Supportato \\ \hline
Pacchetti & Supportato \\ \hline
Commenti & Supportato \\ \hline
\end{tabular}
\end{table}


\section{Struttura del progetto}
Il software è stato scritto interamente in linguaggio Rust. 
L'architettura del transpiler segue una pipeline di elaborazione divisa in tre fasi principali, riportate in figura ed analizzate nei paragrafi seguenti.

Nell'ottica di rendere il software quanto più robusto possibile, si è posta particolare attenzione alla validazione dell'input per ogni fase dell'architettura.
L'approccio è stato quello di continuare la traduzione in casi di errore ben definiti e circoscritti (es: commentando un comando LaTeX non supportato), altrimenti il processo viene arrestato con un errore quanto più chiaro possibile (es: il nome specificato nell'apertura di un environment LaTeX non combacia con quello in chiusura).

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{Latex2Typst.png}
    \caption{struttura progetto}
    \label{fig:placeholder}
\end{figure}


\subsection{Analisi Sintattica - Parsing}

Durante questo processo, il parser si assicura che il file in input, scritto in linguaggio LaTeX (source), abbia una struttura conforme a una grammatica ben specifica. 
Il parser non è stato scritto a mano, bensì generato automaticamente dalla libreria \href{https://docs.rs/pest/2.8.6/pest/}{Pest}, un parser generator per Rust, sulla base della grammatica specificata nei file:
\begin{itemize}
    \item \texttt{/src/latex|\_parser/latex.pest}
    \item \texttt{/src/latex\_parser/latex math.pest}
\end{itemize}
L'aspetto formale di Pest verrà approfondito nel paragrafo seguente.

È riportato di seguito un frammento significativo della grammatica, dove è presente lo \textit{start symbol} \texttt{file} della grammatica prevista.

\begin{verbatim}
file = { SOI ~ item* ~ EOI }
item = { block | command | text | newlines | linebreak | comment }
text = { (!("\\" | NEWLINE | comment) ~ ANY)+ }
\end{verbatim}

L'ordine di definizione delle regole è fondamentale in quanto il parser si fermerà al primo token non terminale che viene rispettato dal lessema analizzato, quindi per esempio: se si considera un lessema che rispetta sia la regola \textit{command}, sia la regola \textit{text}, il parser lo risolverà con la regola \textit{command} poiché presente prima nel pattern.

L'output del parser sarà un ParseTree, ossia un albero che si origina da una singola radice, ordinato, e dove ogni nodo corrisponde a un match tra un lessema del file sorgente e token definito in grammatica.
Ogni nodo rispetta la struttura definita dalla classe \texttt{Pair}, i cui attributi sono:
\begin{itemize}
    \item \texttt{rule}: identificatore della regola grammaticale rispettata 
    \item \texttt{span}:
    \begin{itemize}
        \item \texttt{str}: coordinate esatte del lessame inteso come sottostringa all'interno dell'intero file sorgente rappresentato come un'unica superstringa
        \item \texttt{range}: il range occupato dai caratteri della sottostringa
    \end{itemize}
    \item \texttt{inner}: lista ordinata di elementi figli, quindi altre istanze di \texttt{Pair}
\end{itemize}

L'elenco delle regole grammaticali definite per il parsing del testo è riportato di seguito:
\begin{itemize}
    \item \texttt{file}: intero testo del file di input
    \item \texttt{item}: componente generale del testo
    \item \texttt{name}: nome del comando
    \item \texttt{block}: blocco begin e end
    \item \texttt{command}: comando generale
    \item \texttt{linebreak}: interruzione di riga segnalata in LaTeX come doppio backslash
    \item \texttt{newlines}: interruzione di riga e nuove righe
    \item \texttt{comment}: commento monoriga
    \item \texttt{text}: testo semplice
    \item \texttt{required arg}: argomento obbligatorio del comando
    \item \texttt{argument}: contenuto del \texttt{required arg}
    \item \texttt{arg item}: tipologia di argomento di \texttt{argument}; può essere un altro comando o un testo, permettendo così il nesting dei comandi
    \item \texttt{arg\_text}: testo semplice di un \texttt{argument}
    \item \texttt{optional\_arg}: argomento opzionale del comando
    \item \texttt{optional\_list}: lista di argomenti che possono comporre un argomento opzionale
    \item \texttt{opt\_entry}: argomento singolo della lista
    \item \texttt{kv\_pair}: argomento chiave-valore
    \item \texttt{key}: chiave del \texttt{kv\_pair}
    \item \texttt{value}: valore del \texttt{kv\_pair}, può essere un semplice testo o una lista
    \item \texttt{simple\_value}: valore composto da testo semplice
    \item \texttt{value\_list}: valore composto da una lista di sottovalori
    \item \texttt{sub\_value\_list}: sottovalori della \texttt{value\_list}
    \item \texttt{opt\_item}: argomenti opzionali differenti dalla \texttt{kv\_pair}
    \item \texttt{opt\_text}: testo semplice dell' \texttt{opt\_item}
    \item \texttt{forbidden\_char}: insieme di caratteri da non considerare in determinate regole
\end{itemize}

\subsubsection{Pest}
%% TODO
Pest è un parser generator che utilizza le \textbf{Parsing Expression Grammars} (PEG), ossia delle grammatiche che eliminano l'ambiguità alla radice grazie all'operatore di scelta ordinata `|`: il parser tenta di far corrispondere le espressioni nell'ordine in cui sono definite, selezionando la prima che ha successo.

Le PEG, da definizione, adottano il principio di \textit{packrat parsing} tipicamente presente nell'approccio PEG
Tuttavia Pest,  non adotta

Pest non implementa il backtracking quindi durante l'analisi, un'espressione o ha successo o fallisce: se ha successo, il passo successivo viene eseguito normalmente, ma se fallisce, l'intera espressione fallisce e il parsing verrà interrotto risultando in un errore bloccante per il software.

Citando la documentazione di Pest: "in un sistema con backtracking, si tornerebbe indietro di un passo, “non mangiando” un carattere, e poi si riproverebbe. Ma i PEG non fanno questo. Nella regola \textit{first - second}, una volta che \textit{first} viene analizzato con successo, ha consumato alcuni caratteri che non torneranno mai indietro. \textit{second} può essere eseguito solo sull'input che \textit{first} non ha consumato."
Il parsing attutato tramite pest può essere riassunto cosi: prova a far rispettare la prima espressione, se ha successo continua continua a far rispettare il pattern finché non si conclude, se non ha successo prova con un'altra espressione. Se nessuna espressione ha successo allora genera errore.

Tornando all'esempio della grammatica prima mostrato: come possiamo vedere un file può essere composto da zero o più di un item, la definizione delle regole segue delle espressioni molto simili alle regex, ed a sua volta l'item deve essere composto da uno solo dei diversi token definiti.
Item definisce quindi una regola/pattern generale che identifica ogni elemento presente nel file, successivamente si entra nello specifico di questi token come nella regola \textit{text} che definisce come testo qualsiasi elemento che contenga uno o più caratteri che non inizi con il \textit{backslash}, altrimenti verrebbe identificato con il token del comando, e che non rispetti la regola \textit{NEWLINE} o \textit{comment}.
%% TODO
\subsubsection{Errori sintattici}
Affinché il processo di traduzione sia robusto, il sorgente LaTeX dev'essere ben formato e rispettare la grammatica.
Se il parser incontra un lessema che non riesce a risolvere, l'intero processo viene bloccato e viene prodotto un messaggio di errore in console che fa riferimento alla porzione di input che lo ha generato.

Nella fase di parsing viene effettuato il controllo dei soli \textit{errori sintattici} (o \textit{errori grammaticali}), ossia la sola correttezza sintattica della sequenza di lessemi rispetto alle regole formali definite nella grammatica PEG.
Errori come comandi non riconosciuti oppure argomenti errati sono detti \textit{errori semantici} e saranno identificati più avanti nel processo di traduzione, dove si hanno più informazioni di contesto sull'intero file sorgente.

Alcuni esempi di errori sintattici:
\begin{itemize}
    \item parentesi graffe di un argomento incomplete, solo aperta o solo chiusa
    \item solo apertura o sola chiusura di un environment
    \item apertura e chiusura di due environment differenti
    \item caratteri numerici nella definizione di un environment    
\end{itemize}

\subsection{Analisi Semantica - AST}
Si tratta di uno stadio più avanzato dell'analisi, dove è possibile interpretare le istruzioni scritte in LaTeX e verificarne la loro correttezza in base al contesto in cui si trovano, andando oltre la mera grammatica.

Per rendere più agevole la fase di analisi semantica, il ParseTree ottenuto precedentemente viene sintetizzato in un \textit{Abstract Syntax Tree} (AST).
L'AST è dunque il formato individuato per costruire la \textit{rappresentazione intermedia} del file sorgente. 
A differenza del ParseTree, l'AST è molto più sintetico, espressivo e leggibile, seppur mantenendo lo stesso contenuto informativo del file originario.

Dire che l'AST sia più espressivo del ParseTree sta a significare che i nodi inglobano più informazione al loro interno che può essere di carattere locale (es: informazioni derivati dal fatto che un certo nodo è individuato all'interno in un ambiente LaTeX) o globale (es: informazioni relative al tipo di documento definito a monte del file sorgente).

%% TODO
Viene quindi effettuato un processo di decorazione in cui vengono aggiungente o semplificate informazioni come ad esempio il numero di nuove righe che nel ParseTree vengono rappresentate semplicemente come testo "n", differentemente dall'AST in cui l'oggetto \textbf{NewLineNode} possiede un attributo \textbf{count}. Ad esempio la stringa \textbf{title{LaTeX To Typst}} che nel ParseTree viene scomposta in 7 nodi ognuno padre dell'altro, differentemente nell'AST é rappresentato con solo 3 nodi.

\begin{itemize}
    \item \textbf{CommandNode}, che possiede gli attributi \begin{itemize}
        \item \textbf{Name}, il nome del comando
        \item \textbf{OptionalArgs}, gli argomenti opzionali di un comando racchiusi tra parentesi quadre
        \item \textbf{RequiredArgs}, gli argomenti obbligatori di un comando racchiusi tra parentesi graffe, in questo caso contiene un solo \textbf{RequiredArgNode}, che a sua volta possiede un attributo items di tipo \textbf{Text} \begin{itemize}
            \item \textbf{TextNode}, contiene il valore testuale all'interno delle parentesi quadre.
        \end{itemize}
    \end{itemize}
\end{itemize}

%% TODO

Se la precedente struttura ad albero era basata sulla grammatica, questa nuova struttura ad albero che andiamo a realizzare è basata su una lista di strutture ed enumerativi da noi definiti, nel file \textit{ast structure}, che vengono assegnati ai vari nodi in base alle loro regole grammaticali d'origine.

A titolo di esempio, viene riportata la struttura di un nodo di tipo \texttt{CommandNode} dell'AST:
\begin{verbatim}
pub struct CommandNode {
    pub name: String,
    pub optional_args: Vec<OptionalArgNode>,
    pub required_args: Vec<RequiredArgNode>,
}
\end{verbatim}
Gli attributi \texttt{optional args} e \texttt{required args} sono i vettori che contengono le rispettive liste di argomenti opzionali e obbligatori accettati dal comando, modellati da altre classi.

È evidente quanto sia più compatto e pratico da gestire rispetto al nodo di tipo \texttt{Pair} visto precedentemente nel \textit{ParseTree}.

Nella definizione della struttura dell'AST sono stati definiti 15 tipi di nodi (strutture ed enumerativi):
\begin{itemize}
    \item \texttt{AstDocument}: nodo radice - struct
    \item \texttt{AstItemNode}: nodo generale per una regola - enum
    \item \texttt{BlockNode}: blocco begin/end - struct
    \item \texttt{TextNode}: testo semplice - struct
    \item \texttt{WhitespaceNode}: spazi bianchi che precedono testo - struct
    \item \texttt{NewLinesNode}: numero di nuove righe - struct
    \item \texttt{LinebreakNode}: numero di interuzzione di riga - struct
    \item \texttt{CommentNode}: commenti mono riga - struct
    \item \texttt{CommandNode}: comandi latex - struct
    \item \texttt{RequiredArgNode}: argomenti richiesti dai comandi nelle parentesi graffe - struct
    \item \texttt{ArgItemNode}: oggetto presente nell'argomento richiesto - enum
    \item \texttt{OptionalArgNode}: argomenti opzionali dei comandi nelle parentesi quadre - struct
    \item \texttt{OptionalEntryNode}: oggetto presente nell'argomento opzionale - enum
    \item \texttt{KvPairNode}: argomento definito da una chiave ed un valore - struct
    \item \texttt{OptValueNode}: oggetto presente come valore nel KvPairNode - enum
    \item \texttt{OptItemNode}: oggetto presente nell'argomento opzionale - enum
    \item \texttt{MathNode}: nodo contente la matematica - struct
\end{itemize}

Inoltre, sono stati definiti gli errori semantici come enumerativi. Essi vengono stampati in console in caso di errore durante la fare di generazione dell'AST:
Ecco la trasformazione dei tuoi nodi di errore in una lista LaTeX pronta per essere inserita nel tuo documento:

\begin{itemize}
    \item \texttt{MissingFileNode}
    \item \texttt{MissingItemChild}
    \item \texttt{MissingCommandName}
    \item \texttt{MissingBlockName}
    \item \texttt{MissingRequiredArgItems}
    \item \texttt{MissingOptionalArgEntries}
    \item \texttt{MissingOptionalEntryItems}
    \item \texttt{MissingKeyInKvPair}
    \item \texttt{MissingValueInKvPair}
    \item \texttt{EmptyTextValue}
    \item \texttt{EmptyCommentValue}
    \item \texttt{InvalidNewlineCount}
    \item \texttt{InvalidLinebreakValue}
    \item \texttt{UnexpectedItemRule(Rule)}
    \item \texttt{UnexpectedArgItemRule(Rule)}
    \item \texttt{UnexpectedOptItemRule(Rule)}
    \item \texttt{UnexpectedOptionalEntryRule(Rule)}
    \item \texttt{UnexpectedRule(Rule)}
\end{itemize}

\subsubsection{Errori semantici}
Durante la costruzione dell'AST può succedere che il software possa provare ad estendere l'albero aggiungendo un nuovo nodo foglia che però non rispetta la logica della grammatica; ad esempio aggiungiamo un nodo \textbf{text}, che serve a rappresentare un testo semplice, come nodo figlio di un \textbf{command}. Questa sarebbe un incoerenza nella costruzione dell'AST che potrebbe ad errori nella fase di generazione del codice typst, perciò durante la costruzione effettuiamo sempre controlli per verificare che il nodo che stiamo andando a costruire rispecchi una delle possibili regole selezionate per quel determinato contesto, altrimenti rilascia un errore di \textbf{UnexpecxtedRule}.
Oltre ad un controllo della corretta regola, in questa fase verifichiamo il rispetto di altre condizioni che altrimenti genererebbero problemi nelle fasi successive, come l'assenza del nome del comando oppure, un numero invalido di nuove righe o l'assenza degli argomenti richiesti da un comando. Per quest'ultima tipologia di errore lanciamo un warn sul terminale insieme ad un errore di \textbf{MissingRequiredArgItems}, questa situazione però viene gestita diversamente in base al comando poiché in LaTeX possiamo avere comandi senza argomenti richiesti o con più di uno. Perciò effettuiamo 2 controlli, il primo durante la costruzione del comando per verificare che un comando che necessita di un argomento, come ad esempio textbf, abbia almeno un nodo figlio nel ParseTree che rispetti la regola del \textbf{required args}, in caso negativo il warn ci avvisa che si non si é trovati alcun argomenti. Differente durante la costruzione del required args si controlla che che le parentesi graffe siano popolate, quindi che sia presente una stringa, e non siano vuote. Per effettuare questi controlli é stata creata un'hashmap simile alla TransMap, che restituisce il numero di argomenti minimi attesi per tale argomento, in modo da comprendere se l'assenza di parentesi graffe risulta essere un errore oppure é un comando corretto senza argomenti.
\begin{verbatim}
if let Some(expected) = reqarg_count(&name) {
    if required_args.len() < expected as usize {
        warn!("Comando \\{}: expected {} required arguments, 
            found {}", name, expected, required_args.len());
            
        return Err(SemanticError::MissingRequiredArgItems);
    }
}
\end{verbatim}

\subsection{Generazione del codice Typst - CodeGen}
Come introdotto, l'ultima fase della traduzione é la generazione del nuovo linguaggio mantenendo la semantica invariata; vogliamo quindi lo stesso risultato da parte del nuovo linguaggio. Per fare ciò prendiamo in input l'Abstract Syntax Tree e tramite la navigazione di esso, foglia per foglia, generiamo un codice equivalente, effettuiamo un "rendering"; che nel nostro caso assume il significato di scrivere la traduzione nel file di output.
Il rendering del nuovo codice avviene iterando una funzione di mapping su ogni item dell'AstDocument raccogliendo i risultati che saranno mano mano aggiunti al file. Per ogni nodo foglia su cui iteriamo effettuiamo una funzione di mapping differente poiché bisogna gestire le differenti casistiche; questa scelta é effettuata in base al rispettivo enumerativo del nodo foglia. Un esempio semplice é il rendering del nodo TextNode il cui risultato della funzione di mappamento é una semplice stringa da cui vengono rimosse le parentesi graffe poiché in latex queste non vengono renderizzate nel file pdf differentemente da typst in cui invece compaiono nel documento finale.
\begin{verbatim}
pub(crate) fn render_text(text_node: &TextNode) -> String {
    text_node
        .value
        .chars()
        .filter(|c| *c != '{' && *c != '}')
        .collect()
}
\end{verbatim}
Differentemente per situazioni più complesse come i blocchi ed i comandi é stata implementata un hashmap, denominata \textbf{TransMap}, in cui la chiave, una stringa, é rappresentata dal nome del commando e il valore é la specifica funzione per trattarlo, questa funzione prende come argomenti:
\begin{itemize}
    \item \textbf{name}, stringa contente il nome del comando
    \item \textbf{reqs}, vettore di RequiredArgNode
    \item \textbf{opts}, vettore di OptionalArgNode
    \item \textbf{items}, vettore di AstItemNode. Presente solo nelle funzioni della TransMap dedicata ai blocchi, in modo da passare anche le componenti all'interno dei blocchi e poterle trattare con le corrette funzioni poiché ci troviamo all'interno di un environment differente.
\end{itemize}
Modellando in questa maniera é possibile avere due sole mappe di riferimento per poter indirizzare verso la funzione di rendering corretta in base alla tipologia di funzione ed environment latex. Le due TransMap, avendo la stessa funzione, sono costruite seguendo lo stesso \textbf{"trait"}, che corrisponde ad una comune "interfaccia", implementando la funzione \textbf{translate} che restituisce il risultato del rendering della singola componente da aggiungere in coda alla traduzione.
\begin{verbatim}
pub trait TransMap<T> {
    fn translate(node: &T) -> Option<String>;
}
\end{verbatim}
\subsubsection{Funzionalità e Environment Implementati}
Chiaramente a causa del gran numero di funzioni presenti in LaTeX, per il progetto ci siamo posti l'obiettivo di implementare solamente quelle fondamentali, di uso comune, in modo da avere un software funzionante sui documenti più semplici.
Le funzionalità implementate sono:
\begin{itemize}
    \item \textbf{Text Formatting}: bold, italic, underline, color
    \item \textbf{Text Alignment}: centering, raggedright/left, flushright/left
    \item \textbf{Space and Breaks}: newline, break, hfill/vfill, pagebreak, newpage, clearpage
    \item \textbf{Package}: ragged2e, verbatim, hyperref, listings, graphicx \begin{itemize}
        \item questi pacchetti non essendo necessari in typst, poiché coperti da funzioni built-in, sono semplicemente renderizzati come commenti.
    \end{itemize}
    \item \textbf{Section}: section, subsection, subsubsection, paragraph, subparagraph, title, author, date, today, maketitle, tableofcontents
    \item \textbf{Hyperlinks}: href
\end{itemize}
Invece gli environment implementati sono:
\begin{itemize}
    \item \textbf{Alignment}: center, flushright/left
    \item \textbf{Table}: tabular
    \item \textbf{Comment}: commenti multilinea
    \item \textbf{Text Listing}: liste itemize e enumerate, description
    \item \textbf{Graphics}: figure
    \item \textbf{Code}: verbatim, lstlisting
\end{itemize}

\subsubsection{Error Checking}
Può succedere che il file di input presenti dei comandi la cui gestione non é ancora stata implementata oppure assenti in typst, quindi un tentativo di gestione porterebbe a generare un errore durante il rendering e quindi nel conseguente arresto del software che non restituirebbe nulla in output, questo perché al momento la scrittura sul file di output avviene tutta in unica volta a fine rendering e non riga per riga.
Per evitare tutto ciò quindi sono stati implementati due controlli: uno per i comandi non ancora implementati nella traduzione o erroneamente scritti ed uno per quelli non presenti in typst.
Il primo controllo viene effettuato durante il rendering generico di un comando, quindi quando si cerca la corretta funzione da chiamare, attraverso la TransMap, nel caso non sia presente alcuna key valida, quindi: o il comando desiderato non é ancora stato implementato oppure é stato scritto erroneamente, viene lanciato un warn sul terminale che avvisa l'utente dell'errore ed inoltre renderizza il comando riscrivendo lo stesso messaggio di errore anche sul file di output in modo tale da lasciare una traccia nel file di output dell'errore e poterlo correggere.
\begin{verbatim}
pub(crate) fn render_command(command_node: &CommandNode) -> String {
    if let Some(rendered) = CommandTransMap::translate(command_node) {
        rendered
    } else {
        let out = String::new();
        drop_command_warn(
            COMMANDWARNING::WrongCommandOrNotImplemented(
                command_node.name.clone()),
            Option::from(out),
            Option::from(&*command_node.name),
            Option::from(command_node.required_args.clone()),
        )
    }
}
\end{verbatim}
Differentemente quando il comando non ha una diretta trasposizione su typst, la TransMap, che presenterà tale comando tra le sue key, restituirà la funzione in cui sarebbe dovuto essere implementato gestendo il comando come avviene nella prima situazione: warn sul terminale e comando commentato sul file di output.

\section{Test e Risultati}
Per verificare il corretto funzionamento del software sono stati definiti dei test d'integrazione per ogni componente della CodeGen e per le situazioni che generano errore, in modo da poter controllarne la corretta gestione. Possiamo quindi distinguere due casi di esecuzione del software: 
\begin{itemize}
    \item \textbf{Doc Case}, il caso base in cui il software effettua la sua normale esecuzione di transpiler traducendo il documento latex in input in un file typst e il rendering in pdf.
    \item \textbf{Test Case}, si esegue un rendering di differenti file di input suddividisi per categoria del comando in modo da avere singole unità con cui é più facile verificare il corretto funzionamento della relativa componente che si occupa della generazione del codice typst.
\end{itemize}

\subsection{Esempio Test Case}
Il testing si basa su un controllo del risultato dell'esecuzione di ogni singola componente che prende in input un file che presenta lessemi che ci aspettiamo debbano essere gestiti solamente da quella funzione. Questo controllo si basa sul confronto del risultato con il risultato atteso rappresentato da un file typst che dovrà essere coincidere con l'output per poter asserire il corretto funzionamento. Questa logica vale sia per le componenti di traduzione, sia per le funzioni di gestione degli errori.
Per fare un esempio di input-output delle componenti di traduzione, prendiamo come caso i comandi per la formattazioni del testo.
Input Latex:
\begin{lstlisting}
    \textbf{bold}
    \textit{italic}
    \underline{underline}
    \textcolor{blue}{This is \textbf{bold and blue}.}
\end{lstlisting}
Output Typst:
\begin{lstlisting}
    *Bold*
    _italic_
    #underline[underline]
    #text(blue)[This is *bold and blue*.]
\end{lstlisting}

Nel caso invece della gestione degli errori, nei file di output é atteso un commento che presenta il messaggio d'errore contenente la tipologia d'errore e il lessema che ha generato l'errore.
Input Latex:
\begin{lstlisting}
    \begin{document}
        \commandoInesistente{argomento inesistente}
    \end{document}
\end{lstlisting}
Output Typst:
\begin{lstlisting}
    /*WARN: WrongCommandOrNotImplemented("commandoInesistente"): 
    commandoInesistente \commandoInesistente{argomento inesistente}*/
\end{lstlisting}
Invece un esempio di input che genera un errore che blocca l'esecuzione é:
Input Latex:
\begin{lstlisting}
    \begin{document}
        apertura e chiusura di environment differenti
    \end{article}
\end{lstlisting}
Output sul terminale:
\begin{lstlisting}
    ------------------------- PARSE ERROR -------------------------
    Errore di parsing LaTeX alla riga 9, colonna 1
    \end{article}
    ^
    Atteso: item
    -------------------------
\end{lstlisting}

\subsection{Esempio Document}
Un esempio di documento latex tradotto in typst é questo stesso documento che é stato originalmente scritto in latex e poi tradotto e renderizzato in pdf dal software realizzato.

\section{Conclusioni}
Il presente elaborato ha delineato la struttura e il funzionamento del software e si può concludere che il software é funzionate e svolge correttamente il compito per cui é stato designato, chiaramente allo stato attuale il software é ancora incompleto poiché non tratta molti comandi presenti in latex ed argomenti opzionali per personalizzare i comandi già implementati.

Sviluppi futuri potranno essere sicuramente l'aggiunta dei nuovi comandi nei possibili limiti del rendering di typst.
\end{document}